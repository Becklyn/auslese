dist: trusty
language: node_js

cache:
    directories:
        - $HOME/.cache/Cypress

node_js:
    - 12

script:
    - npm run-script build
    - npm audit
    - npx prettier-package-json package.json --list-different --tab-width 4
    - npm test
